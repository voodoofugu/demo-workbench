import { createStyledAtomStore } from "styled-atom";

const createdStyleAtoms = createStyledAtomStore();

export const workbenchStyleAtoms = createdStyleAtoms;
export const StyledAtom = createdStyleAtoms.StyledAtom;
